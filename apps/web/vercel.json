{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @aurex/web build",
  "installCommand": "echo 'Install handled in buildCommand'",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "regions": ["hnd1"],
  "github": {
    "silent": false
  }
}
